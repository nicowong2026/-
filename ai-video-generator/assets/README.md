# AI Video Generator — Assets

Place static assets here:

- `bgm/`      — Background music files (.mp3)
- `fonts/`    — Subtitle fonts (.ttf/.otf)
- `presets/`  — Style preset JSON files

These files are excluded from `.gitignore` (the `assets/**` exception).
Large model files should NOT be placed here — use a separate storage bucket.
